services:
  app:
    build:
      context: .
      args:
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-https://sofanmore.co.uk}
    image: sofanmore:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
